# Interim V8.2 gate policy and retrospective provenance migration

**Hold:** the methodology owner confirmed that this release over-expands some
path-level uncertainties across sibling locators. Gate counts and assessment gaps
below describe the preserved `adeb691` checkpoint, not final gate publication.
A reviewed patch receipt is pending. Numeric invariance remains verified; no
evidence or audits will be changed to compensate for this runtime bug.

The canonical score remains **85.12%**. Every numeric dimension field, component,
ordinary deduction, denominator, triggered/binding ceiling, weighted contribution,
overall rounding, and diagnostic item grade matches the corrected V8.1 checkpoint
`48bb67b434335cd90002f7bf2078a8a176e2a525`. Both triggered 90% ceilings remain
non-binding. Evaluation validity is **valid**.

Publication readiness is now **not_publication_ready**. V8.2 adds direct gates
for confirmed completely wrong delivered destinations, independent of severity
or rate. The canonical result triggers exactly two gate IDs:

- `GATE-WRONG-LOCATOR`: 87 distinct delivered locators, each frozen as unsupported
  and no-fit with inspectable, source-linked evidence and no affecting uncertainty.
- `GATE-BROKEN-REFERENCE`: two delivered supplementary references,
  `XREF-1FB84F43D7ED` (abolition of slavery) and `XREF-80B3F994D3ED` (9 Thermidor).
  Frozen exception summaries explicitly report absent destinations in the complete
  candidate inventory. Typed `no_valid_destination` encodes those findings;
  null normalizer targets alone were not treated as proof. Original minor
  severities, judgments, confidence, summaries and evidence IDs are unchanged.

These gates cover 89 distinct delivered items. Partial/nonzero fits do not
qualify for the new wrong-locator rule. No severity or judgment was promoted to
force a gate. The originating headings' surviving locators do not repair their
absent delivered reference destinations.

## Assessment limitations

The gate assessment is **indeterminate**, with 2,680
`GATE-ASSESSMENT-LOCATOR-UNCERTAIN` blockers. Eleven preserved structure uncertainty
records identify locator IDs and complete paths. V8.2 excludes all affected
locators, including high-confidence rows on those paths, from direct gate proof.
These assessment gaps are not 2,680 wrong destinations or new candidate defects.
There are no unassessed reference exceptions. Known confirmed gates suffice to
establish non-readiness while the uncertainty gaps remain explicitly disclosed.
Resolving those frozen uncertainties requires separately scoped evidence review;
this migration neither clears them nor reruns their audits.

## Provenance and repair boundary

The Senez evidence/normalization repair is a separate, completed V8.1 checkpoint.
Its 6,222 locator judgments, normalized candidate, inventory, node judgments,
missing-access content, benchmark content, defects and uncertainties are reused.
The only structure additions are the two typed reference resolution objects,
recorded before/after in `target-resolution-changes.json`. Dependent benchmark and
packet hashes are rebound mechanically; no source discovery, benchmark review,
normalization, or substantive locator/missing-access audit was rerun.

`policy-build-input.json` uses the native `retrospective_migration` contract.
The actual migration freeze records `candidate_seen: true`. It preserves the
original candidate-blind V8 policy/freeze and original review/compatibility
approval/release evidence separately, without claiming a new independent review
or approval. The latest corrected V8.1 policy supplies all existing scoring
settings. Its original mechanical `candidate_seen: false` flag is preserved as
historical bytes, not repeated as a claim about migration visibility.

The installed methodology is merged revision
`adeb69171a3278893e8b7ebb3c664ee5bd428efe` (methodology PR49), including native
provenance PR48. All 110 installed files matched the supplied receipt. Identities
are `subject-index-standard-policy-v8.2`, `subject-index-rubric-v8.2`, and
`subject-index-dimension-calculation-v7`. Formula labels change version, while
all numerical calculations remain identical.

The registered structure, score and report commands built `scoring-v82/`.
The existing score-free supplemental-route projection preserves the 70 yellow
review signals; it does not alter arithmetic or gates. The full public bundle
contains all six dimension denominators and assessment/readiness information.

## Preservation and delivery

Original V8 and corrected V8.1 scoring/report bytes remain unchanged in Git.
Private archives preserve exact source/candidate files, prior and current
checkpoints, full Git history, installed methodology and validation logs. Private
locations are excluded from public collections and recorded in the local handoff.
The migration ledger and validation receipt provide public-safe content hashes
and invariance evidence. Website deployment and evaluation PR merge are pending
user approval. Public GitHub push and cross-task private handoff were previously
blocked by automatic approval review and require explicit user approval.
